# ServerAPI

The `server` API provides server-wide management functions.

| Function                       | Description                                                |
|--------------------------------|------------------------------------------------------------|
| server.getOnlinePlayers()      | Returns a table of every currently connected player entity |
| server.stop()                  | Stops the Minecraft server                                 |